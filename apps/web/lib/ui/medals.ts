/** Medal colours for the top three rows of a standings ladder — shared by
 *  the manager ladder and the player rankings tables (both carried a copy
 *  before). Gold is the palette token; silver/bronze are the only values
 *  outside the token set, and only ever used here. */
export const MEDAL = ['var(--gold)', '#C7D0DA', '#C98A5B'];
