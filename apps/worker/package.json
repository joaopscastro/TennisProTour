{
  "name": "@tennis-manager/worker",
  "version": "0.1.0",
  "private": true,
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc --build",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "start": "tsc --build && node dist/index.js"
  },
  "dependencies": {
    "@tennis-manager/api": "^0.1.0",
    "@tennis-manager/application": "^0.1.0",
    "@tennis-manager/domain": "^0.1.0",
    "bullmq": "^5.81.3",
    "ioredis": "^5.11.1"
  },
  "devDependencies": {
    "vitest": "^4.1.10"
  }
}
