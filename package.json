{
  "name": "tennis-manager",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "typecheck": "tsc --build --force && npm run typecheck -w apps/web",
    "setup": "node scripts/setup.js",
    "dev": "node scripts/dev-prepare.js && concurrently -n api,worker,web -c blue,magenta,green \"npm run start -w apps/api\" \"npm run start -w apps/worker\" \"npm run dev -w apps/web\""
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "typescript": "^5.6.0"
  }
}
