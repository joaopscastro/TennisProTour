{
  "name": "tennis-manager",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "typecheck": "tsc --build --force"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
