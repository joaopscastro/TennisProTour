{
  "name": "tennis-manager",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "typecheck": "tsc --build --force && npm run typecheck -w apps/web"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
