node_modules/
dist/
*.tsbuildinfo
data/
.next/
next-env.d.ts
test-results/
playwright-report/
.env
.env.local
.env.*.local
.idea/
balance-report-*.json
playtest-report*.json
soak-report*.json
# agent-played-season harness runs (agentSeason.mjs): digests, decisions,
# apply journals, tick checkpoints, reports. Local artifacts only.
runs/

# ux-probe: session screenshots/journals/logs and any report an agent writes.
# Never commit live session artifacts — only the driver/scripts/README.
tools/ux-probe/.sessions/
tools/ux-probe/reports/
# Per-session Next dev output dirs (see apps/web/next.config.js NEXT_DIST_DIR).
apps/web/.next-ux-*/
